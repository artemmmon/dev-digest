/** Metrics shared by the coloured lines and the textarea over them — they must match exactly. */
export const LINE_HEIGHT = 21;
export const GUTTER_WIDTH = 40;
export const TEXT_PAD_RIGHT = 14;
export const VERTICAL_PAD = 10;
