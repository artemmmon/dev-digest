/** Constants for ConventionCard. */

/** Bounds of a reworded rule — mirror `ConventionPatch.rule` in the shared contract. */
export const RULE_MIN_LENGTH = 3;
export const RULE_MAX_LENGTH = 500;

/** Confidence at or above this is drawn as "solid" (green); below it as "shaky" (amber). */
export const CONFIDENCE_SOLID = 0.85;
