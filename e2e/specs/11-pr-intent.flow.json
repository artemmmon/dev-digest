{
  "name": "PR Overview shows the Intent card empty state",
  "description": "Opens the seeded PR #482 (default tab: Overview) and confirms the INTENT card renders above the description, in its empty state with a Derive intent button. Read-only: no LLM call, nothing is derived.",
  "steps": [
    {
      "cmd": [
        "open",
        "{BASE}/"
      ],
      "label": "load the app root"
    },
    {
      "cmd": [
        "wait",
        "--url",
        "/pulls"
      ],
      "label": "land on the PR list"
    },
    {
      "cmd": [
        "wait",
        "--text",
        "Add rate limiting to public API endpoints"
      ],
      "label": "seeded PR title row is visible (list data loaded)"
    },
    {
      "cmd": [
        "find",
        "text",
        "Add rate limiting to public API endpoints",
        "click"
      ],
      "label": "open the PR row"
    },
    {
      "cmd": [
        "wait",
        "--url",
        "/pulls/482"
      ],
      "label": "navigate to the PR detail route"
    },
    {
      "cmd": [
        "wait",
        "--text",
        "No intent derived yet for this PR"
      ],
      "label": "the Overview tab shows the Intent card's empty-state sentence (no intent stored for the seeded PR)"
    },
    {
      "cmd": [
        "find",
        "role",
        "button",
        "text",
        "--name",
        "Derive intent"
      ],
      "label": "the empty-state Derive intent button is present (read its text, never clicked — no LLM call)"
    }
  ]
}
